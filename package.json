{
    "name": "my-app",
    "version": "1.0.0",
    "description": "",
  "scripts": {
    "build": "npm i && cd server && npm i && cd .. && cd client && npm i && npm run build && npm start",
    "start": "node server/index.js"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.2",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.14.5"
  }
}
